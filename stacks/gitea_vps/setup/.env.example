# ── Required ───────────────────────────────────────────────────────────
# Admin password for the initial Gitea user
GITEA_ADMIN_PASS=changeme

# ── Optional (defaults applied) ────────────────────────────────────────
# GITEA_ADMIN_USER=bruno
# GITEA_ADMIN_EMAIL=bruno@vps
# HERMES_PASS=changeme-on-first-login

# ── Docs webhook (optional — skip if docs stack isn't deployed) ───────
# DOCS_WEBHOOK_SECRET=openssl rand -hex 16
