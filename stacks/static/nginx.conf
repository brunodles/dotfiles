server {
    listen 80;
    server_name static.lab;
    root /usr/share/nginx/html;
    index index.html;

    # Enable directory listing
    autoindex on;
    autoindex_exact_size off;
    autoindex_localtime on;

    location / {
        try_files $uri $uri/ =404;
    }
}
