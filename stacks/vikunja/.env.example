# ── Required ───────────────────────────────────────────────────────────
# Admin password for Vikunja
VIKUNJA_ADMIN_PASS=changeme

# JWT secret — generate with: openssl rand -base64 32
VIKUNJA_JWT_SECRET=

# ── Optional (defaults applied) ───────────────────────────────────────
# VIKUNJA_ADMIN_USER=bruno
# VIKUNJA_ADMIN_EMAIL=bruno@vps
